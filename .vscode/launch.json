{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lua-local",
            "request": "launch",
            "name": "Debug",
            "program": {
                "command": "love"
            },
            "args": [ "${env:KRISTAL_PATH}", "--mod", "wii_kristal" ],
            "scriptRoots": ["${env:KRISTAL_PATH}", "."]
        }
    ]
}